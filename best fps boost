local Rayfield = loadstring(game:HttpGet('https://sirius.menu/rayfield'))()
local Window = Rayfield:CreateWindow({
   Name = "GOJO AUTO HATCH 🟣",
   LoadingTitle = "Gojo Domain Expansion...",
   LoadingSubtitle = "by Tuan Nando",
   Theme = "Purple"
})

local EggTab = Window:CreateTab("Auto Hatch", 4483362458)

_G.AutoHatch = false

EggTab:CreateToggle({
   Name = "Huge Hunter (Auto Hatch)",
   CurrentValue = false,
   Callback = function(Value)
      _G.AutoHatch = Value
      if Value then
         task.spawn(function()
            while _G.AutoHatch do
               pcall(function()
                  game:GetService("ReplicatedStorage").Network.Eggs_RequestPurchase:InvokeServer("Spawn", 1)
               end)
               task.wait(0.005)
            end
         end)
      end
   end,
})

Rayfield:Notify({Title = "GOJO V1 LOADED", Content = "Auto Hatch Ready", Duration = 5})
